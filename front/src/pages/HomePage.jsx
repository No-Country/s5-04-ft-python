import { Stack } from '@mui/material'
import { useAuth } from '../hooks/useAuth'

const HomePage = () => {
    return (
        <Stack>
            <h1>HomePage</h1>
        </Stack>
    )
}

export default HomePage
